// A touch has to be held briefly before it picks a card or column up, so a swipe
// that starts on one scrolls the board instead of dragging.
export const BOARD_DRAG_START_DELAY = { touch: 250, mouse: 0 };
