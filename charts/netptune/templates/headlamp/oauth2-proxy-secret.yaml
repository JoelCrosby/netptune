{{- if .Values.headlamp.enabled }}
---
apiVersion: "v1"
kind: "Secret"
metadata:
  name: "oauth2-proxy-secrets"
  labels:
    app.kubernetes.io/name: "netptune-app"
    app.kubernetes.io/component: "headlamp"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
stringData:
  OAUTH2_PROXY_CLIENT_ID: "{{ .Values.secrets.headlamp.github_client_id }}"
  OAUTH2_PROXY_CLIENT_SECRET: "{{ .Values.secrets.headlamp.github_client_secret }}"
  OAUTH2_PROXY_COOKIE_SECRET: "{{ .Values.secrets.headlamp.cookie_secret }}"
type: "Opaque"
{{- end }}
