{{- if .Values.parameters.api.autoscaling.enabled }}
---
apiVersion: "autoscaling/v2"
kind: "HorizontalPodAutoscaler"
metadata:
  name: "api-hpa"
  labels:
    app.kubernetes.io/name: "netptune-app"
    app.kubernetes.io/component: "api"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
spec:
  scaleTargetRef:
    apiVersion: "apps/v1"
    kind: "Deployment"
    name: "api-deployment"
  minReplicas: {{ .Values.parameters.api.autoscaling.minReplicas | int }}
  maxReplicas: {{ .Values.parameters.api.autoscaling.maxReplicas | int }}
  metrics:
    - type: "Resource"
      resource:
        name: "cpu"
        target:
          type: "Utilization"
          averageUtilization: {{ .Values.parameters.api.autoscaling.targetCPUUtilizationPercentage | int }}
{{- end }}
