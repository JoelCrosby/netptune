---
apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "api-deployment"
  labels:
    app.kubernetes.io/name: "netptune-app"
    app.kubernetes.io/component: "api"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/name: "netptune-app"
        app.kubernetes.io/component: "api"
        app.kubernetes.io/instance: "{{ .Release.Name }}"
    spec:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: "kubernetes.io/hostname"
          whenUnsatisfiable: "ScheduleAnyway"
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: "netptune-app"
              app.kubernetes.io/component: "api"
              app.kubernetes.io/instance: "{{ .Release.Name }}"
      containers:
        - image: "{{ .Values.parameters.api.api_image }}"
          name: "api"
          env:
            - name: "ASPNETCORE_ENVIRONMENT"
              value: "Production"
          envFrom:
            - configMapRef:
                name: "api-config"
            - secretRef:
                name: "api-secrets"
          ports:
            - name: "http"
              protocol: "TCP"
              containerPort: {{ .Values.parameters.api.port_http | int }}
          imagePullPolicy: "Always"
          startupProbe:
            httpGet:
              path: "/health/deps"
              port: "http"
            initialDelaySeconds: 15
            periodSeconds: 5
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: "/health/live"
              port: "http"
            initialDelaySeconds: 10
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: "/health/ready"
              port: "http"
            initialDelaySeconds: 10
            periodSeconds: 15
            failureThreshold: 3
          resources:
{{- toYaml .Values.parameters.api.resources | nindent 12 }}
  selector:
    matchLabels:
      app.kubernetes.io/name: "netptune-app"
      app.kubernetes.io/component: "api"
      app.kubernetes.io/instance: "{{ .Release.Name }}"
  replicas: {{ .Values.parameters.api.replicas | int }}
  revisionHistoryLimit: 3
  {{- /* maxSurge 0: at one replica the cluster has no spare CPU for a surge pod, so a
       surge-based rollout cannot schedule. Restore maxSurge 1 once nodes have headroom. */}}
  strategy:
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
    type: "RollingUpdate"
