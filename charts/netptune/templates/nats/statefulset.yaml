---
apiVersion: "apps/v1"
kind: "StatefulSet"
metadata:
  name: "nats-statefulset"
  labels:
    app.kubernetes.io/name: "netptune-app"
    app.kubernetes.io/component: "nats"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
spec:
  serviceName: "nats-service"
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: "netptune-app"
      app.kubernetes.io/component: "nats"
      app.kubernetes.io/instance: "{{ .Release.Name }}"
  template:
    metadata:
      labels:
        app.kubernetes.io/name: "netptune-app"
        app.kubernetes.io/component: "nats"
        app.kubernetes.io/instance: "{{ .Release.Name }}"
    spec:
      # The nats image runs as root and defines no nats user. fsGroup is what makes the claim
      # group-writable, so the uid below can create the jetstream store directory.
      securityContext:
        fsGroup: 1000
        runAsUser: 1000
      containers:
        - name: "nats"
          # Pinned, not `nats:alpine`: in-place filter-subject updates to an existing durable consumer
          # need nats-server >= 2.10, and a floating tag can silently move under that floor.
          image: "nats:2.14.3-alpine"
          args:
            - "-js"
            # Without an explicit store dir jetstream writes to the container filesystem, leaving the
            # stream ephemeral even with a claim attached.
            - "-sd"
            - "/data"
            - "-m"
            - "8222"
          ports:
            - name: "client"
              protocol: "TCP"
              containerPort: 4222
            - name: "monitor"
              protocol: "TCP"
              containerPort: 8222
          imagePullPolicy: "IfNotPresent"
          volumeMounts:
            - name: "nats-data"
              mountPath: "/data"
          livenessProbe:
            httpGet:
              path: "/healthz"
              port: "monitor"
            initialDelaySeconds: 10
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: "/healthz"
              port: "monitor"
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              memory: "512Mi"
  volumeClaimTemplates:
    - metadata:
        name: "nats-data"
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: "{{ .Values.persistence.nats.size }}"
  updateStrategy:
    type: "RollingUpdate"
    rollingUpdate:
      maxUnavailable: 1
