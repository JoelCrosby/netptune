---
apiVersion: "v1"
kind: "Secret"
metadata:
  name: "cache-secrets"
  labels:
    app.kubernetes.io/name: "netptune-app"
    app.kubernetes.io/component: "cache"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
stringData:
  VALKEY_PASSWORD: "{{ .Values.secrets.cache.cache_password }}"
type: "Opaque"
