{{- if .Values.dbgate.enabled }}
---
apiVersion: "v1"
kind: "Secret"
metadata:
  name: "dbgate-secrets"
  labels:
    app.kubernetes.io/name: "netptune-app"
    app.kubernetes.io/component: "dbgate"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
stringData:
  CONNECTIONS: "con1,con2"
  LABEL_con1: "Netptune"
  ENGINE_con1: "postgres@dbgate-plugin-postgres"
  SERVER_con1: "postgres-service"
  PORT_con1: "5432"
  DATABASE_con1: "{{ .Values.config.app.POSTGRESDB_DATABASENAME }}"
  USER_con1: "{{ .Values.config.postgres.POSTGRES_USER }}"
  PASSWORD_con1: "{{ .Values.secrets.postgres.postgres_password }}"
  LABEL_con2: "Netptune Cache"
  ENGINE_con2: "redis@dbgate-plugin-redis"
  SERVER_con2: "cache-service"
  PORT_con2: "6379"
  PASSWORD_con2: "{{ .Values.secrets.cache.cache_password }}"
type: "Opaque"
{{- end }}
