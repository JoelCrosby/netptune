{{- if .Values.pgadmin.enabled }}
---
apiVersion: "v1"
kind: "Secret"
metadata:
  name: "pgadmin-oauth2-proxy-secrets"
  labels:
    app.kubernetes.io/name: "netptune-app"
    app.kubernetes.io/component: "pgadmin-oauth2-proxy"
    app.kubernetes.io/instance: "{{ .Release.Name }}"
stringData:
  OAUTH2_PROXY_CLIENT_ID: "{{ .Values.secrets.pgadmin.github_client_id }}"
  OAUTH2_PROXY_CLIENT_SECRET: "{{ .Values.secrets.pgadmin.github_client_secret }}"
  OAUTH2_PROXY_COOKIE_SECRET: "{{ .Values.secrets.pgadmin.cookie_secret }}"
type: "Opaque"
{{- end }}
